package main

import "fmt"

func main(){
	// constant value - means not changeable and for read-only purposes
	const NUMBER int = 10
	const NAME string = "Decimo"

	fmt.Println("Constants")
	fmt.Println(NUMBER)
	fmt.Println(NAME)
}